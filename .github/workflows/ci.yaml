name: ci

on:
  push:
    branches: [main, 'renovate/**']
  pull_request:
  workflow_dispatch:

concurrency:
  group: '${{ github.workflow }}-${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

env:
  FORCE_COLOR: 3
  LOG_LEVEL: debug

jobs:
  validate-config-presets:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        file:
          - default
    env:
      RENOVATE_CONFIG_FILE: ${{ matrix.file }}.json
    steps:
      - uses: actions/checkout@755da8c3cf115ac066823e79a1e1788f8940201b # v3.2.0
      - name: Validating ${{ env.RENOVATE_CONFIG_FILE }}
        run: npx -p renovate renovate-config-validator

  # Revisit this job when renovatebot/renovate#18974 is resolved.
  # renovate:
  #   needs: validate-config-presets
  #   if: github.event_name != 'workflow_dispatch'
  #   uses: ./.github/workflows/renovate.yaml
  #   # Set autodiscover to false to run Renovate on this repository only
  #   with:
  #     autodiscover: false
  #     print_config: true
  #   secrets: inherit
